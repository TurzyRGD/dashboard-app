# Zależności (pobierane przez npm install)
node_modules/

# Pliki budowania (generowane przez npm run build)
.next/
out/
build/

# DANE I WGRANE PLIKI (Kluczowe dla Twojej architektury)
# Ignorujemy je, bo będą przechowywane w wolumenach Dockera na Proxmoxie
data/db.json
public/uploads/

# Logi i pliki tymczasowe
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Środowisko (opcjonalnie, jeśli będziesz używać plików .env)
.env*.local